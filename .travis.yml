sudo: required
services:
  - docker

before_install:
  - docker build -t pavanmaddali/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true pavanmaddali/docker-react npm test -- --coverage
